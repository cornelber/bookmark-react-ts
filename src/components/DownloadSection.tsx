import React from "react";
import SectionHeader from "./SectionHeader";
import CardList from "./CardList";

const DownloadSection = () => {
  return (
    <section className="download-section mt-28 mb-48">
      <SectionHeader />
      <CardList />
    </section>
  );
};

export default DownloadSection;
